###############################
# CDSS Backend environment
# Copy to .env and fill values for local/dev.
# Render: set these as Environment Variables in the service dashboard.
###############################

# Application
CDSS_APP_NAME=CDSS Backend
# Set True locally to get richer error messages
CDSS_DEBUG=False
# API bearer token used for admin and sync push endpoints
CDSS_API_TOKEN=ee3b0d29889ea8211f87b5bc5e9bdee2
CDSS_JWT_SECRET=95d187b88760ce6cfb97fe346db2a031
CDSS_ACCESS_EXPIRE_MINUTES=30
CDSS_REFRESH_EXPIRE_MINUTES=43200
CDSS_FRONTEND_ORIGIN=http://localhost:3000

# PostgreSQL connection string (SQLAlchemy compatible)
# Production (Neon)
DATABASE_URL=postgresql://neondb_owner:npg_fS3cKZtxmQ8L@ep-lingering-sound-a16m8q32-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Local development DB (uncomment to use locally)
# DATABASE_URL=postgresql://cdss_user:strongpassword@localhost/cdss_db

# Optional: override Alembic url when running migrations locally
#SQLALCHEMY_URL=${DATABASE_URL}
